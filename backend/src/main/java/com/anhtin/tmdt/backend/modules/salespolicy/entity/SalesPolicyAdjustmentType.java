package com.anhtin.tmdt.backend.modules.salespolicy.entity;

public enum SalesPolicyAdjustmentType {
    PERCENTAGE,      // Theo tỷ lệ phần trăm (%)
    FIXED_AMOUNT,    // Theo giá trị tiền mặt cố định
    SPECIFIC_PRICE   // Theo giá chỉ định trực tiếp
}
